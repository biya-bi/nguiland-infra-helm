apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "artifactory-common.fullname" . }}-extra-cleanup-scripts
  annotations:
    kustomize.toolkit.fluxcd.io/substitute: "disabled"
data:
  delete-snapshots.sh: |
{{ include "artifactory-oss.delete-snapshots-script" . | indent 4 }}
