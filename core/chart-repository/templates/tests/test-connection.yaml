apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-repository.fullname" . }}-test-connection"
  labels:
    {{- include "chart-repository.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-repository.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
