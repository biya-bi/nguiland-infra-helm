apiVersion: v1
kind: Service
metadata:
  name: {{ include "flux-gitrepository-tekton-trigger.fullname" . }}
  labels:
    {{- include "flux-gitrepository-tekton-trigger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flux-gitrepository-tekton-trigger.selectorLabels" . | nindent 4 }}
